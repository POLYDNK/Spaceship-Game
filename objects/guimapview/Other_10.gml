/// @desc Center map to active solar system

var current_solar = global.galaxy[global.current_solar_system];
mapX = current_solar.x_pos;
mapY = current_solar.y_pos;

prev_mouse_x = 0;
prev_mouse_y = 0;
